# Excel Exports

These CSV exports are designed to open directly in Microsoft Excel:

- `kpi_summary.csv`: headline metrics.
- `category_summary.csv`: category revenue and order-value view.
- `segment_summary.csv`: customer-segment performance view.

The files are generated by `python src/analyze.py`.
