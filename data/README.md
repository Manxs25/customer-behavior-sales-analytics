# Data Notes

`customer_shopping_behavior.csv` is retained as an input dataset only. The new project does not reuse the source repository's notebook, SQL, README, dashboard, or implementation.

The source repository included this dataset under an MIT license. The applicable copyright and license notice is preserved in `THIRD_PARTY_NOTICES.md`. The generated clean dataset and summaries are produced by `src/analyze.py`.
